# DepQBF + QRPcert (certificate generation)

Not committed as prebuilt binaries (host-specific). Build from **official source**:

```bash
bash scripts/setup_depqbf_cert.sh
```

| Binary | Upstream |
|--------|----------|
| `depqbf` | https://github.com/lonsing/depqbf (v6.03) |
| `qrpcert` | https://fmv.jku.at/qrpcert/ (QRPcert 1.0.1) |

See [`docs/CERTIFICATES.md`](../../docs/CERTIFICATES.md).
