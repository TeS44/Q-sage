"""
Paper-identical QCIR builders (official implementation for bwnib + pg).

Self-contained under ``qsage.encode.paper`` — no imports from ``legacy/``.
Used by ``qsage.encode.bwnib`` and ``qsage.encode.positional`` (``pg``).
"""
