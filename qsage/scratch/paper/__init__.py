"""
Paper-identical QCIR builders (vendored & cleaned algorithm from the
original paper code).  Self-contained: no imports from ``legacy/``.

Used by ``qsage.scratch.hex`` / ``qsage.scratch.grid`` so public scratch
API emits the same QCIR (and gate count) as paper goldens / previous.
"""
